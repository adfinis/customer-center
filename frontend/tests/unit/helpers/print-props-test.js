import { printProps } from 'adsycc/helpers/print-props';
import { module, test } from 'qunit';

module('Unit | Helper | print props');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = printProps([42]);
  assert.ok(result);
});
