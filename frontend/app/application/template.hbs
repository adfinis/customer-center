{{#if session.isAuthenticated}}
  {{#adsy-navigation}}
    <main class="page-content page-content--scroll">
      {{outlet}}
    </main>
  {{/adsy-navigation}}
{{else}}
  <main class="page-content">
    {{liquid-outlet}}
  </main>
{{/if}}

{{ember-notify messageStyle='bootstrap' closeAfter=5000}}
