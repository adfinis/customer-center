import { symonState } from '../../../helpers/symon-state';
import { module, test } from 'qunit';

module('Unit | Helper | symon state');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = symonState(42);
  assert.ok(result);
});
